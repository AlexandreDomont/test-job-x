pipeline {
	agent any
 	environment {
        	GO111MODULE = 'auto'
    	}	
	tools {
		go 'go-1.20.1'
	}
	stages {
		stage('Build') {
			steps { sh 'go run math/math.go'}
			}
		
		stage ('Test unit') {
			steps {sh 'go test math/'}
		}
	}
}

